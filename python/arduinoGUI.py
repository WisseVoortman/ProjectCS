from tkinter import *
from tkinter import ttk

class GUI:
    def __init__(self):
        print("start ArduinoGUI")

        # create a frame
        # put all required elements on the frame
        # add the frame to the mainframe with pack()

        #frame = ttk.Frame(, padding="3 3 12 12")  # left top right bottem    create mainframe in Root winow
        #label = Label(self.frame, text="zie hier het arduinoGUI label")
        #label.pack()
        #arduinoGUI.frame.grid(column=0, row=0, sticky=(N, W, S, E))
        print("end ArduinoGUI")